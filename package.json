{
  "name": "rifas-plataforma",
  "version": "1.0.0",
  "private": true,
  "description": "Plataforma moldeable de rifas, captación de leads y propuestas",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently -n api,web -c cyan,magenta \"npm run dev -w backend\" \"npm run dev -w frontend\"",
    "dev:backend": "npm run dev -w backend",
    "dev:frontend": "npm run dev -w frontend",
    "build": "npm run build -w frontend",
    "build:all": "npm install && npm run build -w frontend && npm install -w backend",
    "render:build": "npm install && npm run build -w frontend && npm install -w backend",
    "start": "npm run start -w backend",
    "start:production": "cd backend && npm start",
    "supabase:test": "node scripts/setup-supabase.mjs"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}