services:
  - type: web
    name: campa-a-de-paginas-ia
    runtime: node
    plan: free
    rootDir: frontend
    buildCommand: npm install && VITE_API_URL= npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
